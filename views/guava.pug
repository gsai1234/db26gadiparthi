extends layout

block content
  h1= title
  - let guava = results;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row m-1')
      each m, index in guava 
       - let destination = `dispatch(\"/guava/update?id=${m._id}\")`
       div(class="row-sm-2", class="m-1") Item #{m.guava_type}(#{m.weight}) is #{m.cost}
        button.btn( class="btn-primary ml-1", onclick= destination) Update

  script. 
    function dispatch(path){ 
      window.location.href=path 
    }